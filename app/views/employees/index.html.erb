<p id="notice"><%= notice %></p>

<h1>Employees</h1>

<table id="jqGrid"></table>
<div id="jqGridPager"></div>

<script type="text/javascript">

  $(document).ready(function () {
    $("#jqGrid").jqGrid({
      url: "/employees",
      datatype: "json",
      mtype: 'GET',
      colNames: ['ID#', 'Name', 'Title', 'Status', 'Type', 'Attendance Rate', 'Rate', 'Location', 'capitalizable_group_id','Start Date', 'End Date'],
      colModel: [
        {
          name: "id",
          index: "id",
          width: 10
        },
        {
          name: 'name',
          index: "name",
          width: 100,
          editable: true // must set editable to true if you want to make the field editable
        },
        {
          name: 'title',
          index: "title",
          width: 30,
          editable: true
        },
        {
          name: 'status',
          index: "id",
          width: 30,
          editable: true
        },
        {
          name: 'employment_type',
          index: "employment_type",
          width: 30,
          editable: true
        },
        {
          name: 'attendance_rate',
          index: "attendance_rate",
          width: 30,
          editable: true
        },
        {
          name: 'hourly_rate',
          index: "hourly_rate",
          width: 20,
          editable: true
        },
        {
          name: 'location',
          index: "location",
          width: 20,
          editable: true
        },{
          name: 'capitalizable_group_id',
          index: "capitalizable_group_id",
          width: 200,
          editable: true
        },{
          name: 'commencement_date',
          index: "commencement_date",
          width: 200,
          editable: true
        },{
          name: 'termination_date',
          index: "termination_date",
          width: 200,
          editable: true
        }
      ],
      pager: "#jqGridPager",
      rowNum: 5,
      rowList : [10, 20, 30],
      sortname : 'id',
      viewrecords: true,

      //onSelectRow: editRow, // the javascript function to call on row click. will ues to to put the row in edit mode

    }).jqGrid("navGrid", "#jqGridPager", {
      edit: true,
      add: true,
      del: true
    }, {url: "/employees/update"}, {url: "/employees/create"}, {url: "employees/destroy/"}, {
      closeAfterEdit: true,
      closeOnEscape: true
    });

    /*
     var lastSelection;

     function editRow(id) {
     if (id && id !== lastSelection) {
     var grid = $("#jqGrid");
     grid.jqGrid('restoreRow', lastSelection);
     grid.jqGrid('editRow', id, {keys: true});
     lastSelection = id;
     }
     }
     */
  });

</script>

